  <button>Inscribirse</button>

<style>

  button {
    padding: 12px 3em;
    color: var(--white);
    font-weight: bold;
    font-size: 18px;
    background-color: var(--primary);
    transition: filter .15s ease-in;
  }

  button:hover {
    filter: opacity(0.4);
}
</style>